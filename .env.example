DATABASE_URL=postgres://user:password@localhost:5432/reportbot
PORT=3000

# Email delivery (SMTP)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=reportbot@example.com

# Google Sheets (optional - can also use source_config per pipeline)
GOOGLE_APPLICATION_CREDENTIALS=

# Slack delivery (per-pipeline via delivery_config.webhook_url)
